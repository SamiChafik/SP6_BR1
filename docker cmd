Build your Spring Boot application first (creates the JAR file):

# bash
mvn clean package
Build and start the containers:

# bash
docker-compose up -d
To stop the containers:

# bash
docker-compose down
To rebuild the images (after making changes):

# bash
docker-compose up -d --build